export type CardType = {
    quantity: number,
    color: string,
    title: string, 
    icon: string, 
}

export type ProgressItemType = {
    title: string,
    items: string,
    progress: number,
    buttonLabel: string
}